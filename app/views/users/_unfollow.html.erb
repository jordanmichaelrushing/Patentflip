<%= form_for(current_user.relationships.find_by_followed_id(@users),
              html: { method: :delete },
              remote: true) do |f| %>
              <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Unfollow", class: "btn btn-mini btn-primary", id: "sign-show-unfollow" %>
<% end %>