<% provide(:title, 'All Users') %>
	<div class="outtie">
    <div class="innie">
			<h1>All Users</h1>
      <ul class="users">
        <%= render "userlist" %>
      </ul>
        <loading>
          <%= will_paginate %>
        </loading>
		</div>
	</div>