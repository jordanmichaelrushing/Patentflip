<% @users ||= current_user %>
  <div class="stats">
    <a href="<%= following_user_path(@users) %>">
      <strong id="following" class="stat">
        <%= @users.followed_users.count %>
      </strong>
      following
    </a>
    <a href="<%= followers_user_path(@users) %>">
      <strong id="followers" class="stat">
        <%= @users.followers.count %>
      </strong>
      followers
    </a>
  </div>