<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.text_area :content, placeholder: "Post about what you're working on...", id: 'my-form' %>
	<%= f.submit "Post", class: "btn btn-micro btn-primary", id: "home_post" %>
<% end %>