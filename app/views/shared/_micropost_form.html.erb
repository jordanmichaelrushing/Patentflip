<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.text_area :content, placeholder: "Compose new micropost...", id: 'pussy' %>
	<%= f.submit "Post", class: "btn btn-large btn-primary", id: "btn_align" %>
<% end %>